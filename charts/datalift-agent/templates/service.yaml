apiVersion: v1
kind: Service
metadata:
  name: {{ include "datalift.fullname" . }}
  labels:
    {{- include "datalift.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datalift.selectorLabels" . | nindent 4 }}
